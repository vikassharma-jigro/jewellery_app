void main() async {
  // Try to read token from shared preferences file if possible, or just look at the app source code
  // Wait, I don't have the password for login. Let's just grep the backend or print what the app has.
}
