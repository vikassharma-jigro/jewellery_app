import 'package:flutter/material.dart';

class NewStockScreen extends StatelessWidget {
  const NewStockScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text("Under Construction")),
      );
}
